import React, { Component } from 'react';

const breweryMarker = (props) => (
    <div style={{
        width: '100px',
        height: '100px',
        background: 'red'
    }} />
);

export default breweryMarker;
